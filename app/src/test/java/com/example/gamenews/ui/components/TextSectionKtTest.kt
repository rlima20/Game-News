/*
package com.example.gamenews.ui.components

import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.assertTextEquals
import androidx.compose.ui.test.onNodeWithText
import com.example.gamenews.model.GameNewsState
import org.junit.Rule
import org.junit.Test

class TextSectionKtTest {

    @Test
    fun testTextSection() {
        val searchedWord = "example"

        val gameNewsState = GameNewsState(
            title = "Example Title",
            date = "June 27, 2023",
            description = "Example description",
            link = "https://example.com"
        )

        composeTestRule.setContent {
            TextSection(searchedWord, gameNewsState)
        }

        // Assert the title text
        composeTestRule.onNodeWithText("Example Title").assertIsDisplayed()
        composeTestRule.onNodeWithText("Example Title").assertTextEquals(gameNewsState.title)
*/
/*
        // Assert the date text
        composeTestRule.onNodeWithText("June 27, 2023").assertIsDisplayed()
        composeTestRule.onNodeWithText("June 27, 2023").assertTextEquals(gameNewsState.date)

        // Assert the description text
        composeTestRule.onNodeWithText("Example description").assertIsDisplayed()
        composeTestRule.onNodeWithText("Example description")
            .assertTextEquals(gameNewsState.description)

        // Assert the link text
        composeTestRule.onNodeWithText("https://example.com").assertIsDisplayed()
        composeTestRule.onNodeWithText("https://example.com").assertTextEquals(gameNewsState.link)*//*

    }
}
*/
